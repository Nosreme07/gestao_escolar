import 'package:flutter/material.dart';

class CoresDomex {
  // Cores Principais - Sérias e Tecnológicas
  static const Color azulPrincipal = Color(
    0xFF1A237E,
  ); // Indigo escuro (Confiança/Tech)
  static const Color cinzaFundo = Color(0xFFF5F5F5); // Cinza bem claro (Leveza)

  // Cores de Ação e Destaque
  static const Color laranjaAcao = Color(
    0xFFFF9800,
  ); // Laranja (Energia para botões importantes)
  static const Color verdeSucesso = Color(
    0xFF4CAF50,
  ); // Verde (Confirmar, Pago)

  // Texto e Detalhes
  static const Color textoPrincipal = Color(0xFF212121);
  static const Color textoSecundario = Color(0xFF757575);
}
