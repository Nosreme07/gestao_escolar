name: gestao_escolar
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.8
  image_picker: ^0.8.5+3
  mask_text_input_formatter: ^2.9.0
  firebase_core: ^4.5.0
  cloud_firestore: ^6.1.3
  firebase_storage: ^13.1.0
  cross_file: ^0.3.3 
  desktop_drop: ^0.7.0
  
  # PACOTES DE PDF E LINKS
  url_launcher: ^6.3.0
  pdf: ^3.11.1
  printing: ^5.13.0
  
  # NOVO: PACOTE PARA ENQUADRAR/CORTAR A FOTO
  image_cropper: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true